"""lib — shared data-access for the marketing dashboard (Postgres migration).

Starts with db + queries used by the weekly-sales fix; the rest of the
Streamlit report's lib modules (taxonomy, deals, views, …) can land here as we
migrate the full dashboard onto Postgres.
"""
