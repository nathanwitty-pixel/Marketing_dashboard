# Self-made vs running combos — Python dependencies
pandas>=2.0
SQLAlchemy>=2.0
psycopg2-binary>=2.9      # Postgres driver (Odoo POS source database)
python-dotenv>=1.0
gspread>=6.0             # Google Sheets (offer sheet + deals sheet)
google-auth>=2.0
google-auth-oauthlib>=1.0  # only needed for the OAuth desktop fallback in google_auth.py
